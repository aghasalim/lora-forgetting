.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.env

# Regenerated by `make data` from a fixed seed.
data/*.jsonl

# The adapter is committed: 17 MB, and the whole point of LoRA is that the
# artefact is small enough to ship. Base weights are not, and come from the Hub.
artifacts/**/checkpoint-*
